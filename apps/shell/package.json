{
  "name": "@psp-emulator/shell",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The XMB front-end. Runs inside the Tauri webview and as the web demo build.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  }
}
