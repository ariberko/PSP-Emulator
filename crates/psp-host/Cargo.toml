[package]
name = "psp-host"
description = "Settings, emulator discovery and game launching for the desktop shell"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
psp-metadata = { path = "../psp-metadata" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3"
# The bundled-ROM tests install real, parseable containers and then scan them, so
# they need the fixture builders.
psp-metadata = { path = "../psp-metadata", features = ["testkit"] }
